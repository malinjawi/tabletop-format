# Card design source

Each physical component family is versioned separately and compiles with the shared trim, paper, and palette system. `templates/layout.yaml` is the portable monolithic fallback used for interchange and regression checks.
