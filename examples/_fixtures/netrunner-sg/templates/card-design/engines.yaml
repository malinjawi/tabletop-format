version: 1
active: forge-native
engines:
  - id: forge-native
    type: forge-native
    label: Forge native
    description: The current production renderer. Its compiled output remains the release source of truth.
    source: templates/card-design/manifest.yaml
    status: active
    families:
      - agenda
      - asset
      - event
      - hardware
      - ice
      - corp-identity
      - runner-identity
      - operation
      - program
      - resource
      - upgrade
  - id: pnpink-working-copy
    type: pnpink
    label: Inkscape + PnPInk working copy
    description: A version-pinned external production source for every card family. Card data and the SVG template return through Forge review; it is not the active release renderer.
    source: templates/card-design/experiments/pnpink/manifest.yaml
    status: beta
    families:
      - agenda
      - asset
      - event
      - hardware
      - ice
      - corp-identity
      - runner-identity
      - operation
      - program
      - resource
      - upgrade
