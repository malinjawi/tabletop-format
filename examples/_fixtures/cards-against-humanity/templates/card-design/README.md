# Card design source

The rendered cards compile from two family files plus one shared physical-card component. The monolithic `templates/layout.yaml` remains an interchange fallback and is regression-tested against the composed output.
