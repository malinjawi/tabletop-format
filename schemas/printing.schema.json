{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://spec.example.dev/schemas/printing.schema.json",
  "title": "Printing (physical appearance)",
  "description": "A specific physical/visual appearance of a Card in a Set: art, flavor, collector number, variant. Many printings may point at one card. Files: components/printings.json is an array of these.",
  "type": "object",
  "required": ["id", "card_id", "set_id"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "description": "Stable opaque printing ID.",
      "pattern": "^[a-z0-9][a-z0-9_]{1,63}$"
    },
    "card_id": {
      "type": "string",
      "description": "The rules identity this printing embodies. MUST exist in cards.json."
    },
    "set_id": {
      "type": "string",
      "description": "MUST exist in sets."
    },
    "collector_number": {
      "type": "string",
      "description": "Human-readable position within the set ('042', '042a'). String, not integer — promo suffixes exist."
    },
    "quantity": {
      "type": "integer",
      "minimum": 1,
      "default": 1,
      "description": "Physical copies of this printing included in the set."
    },
    "artist": { "type": "string" },
    "flavor_text": { "type": "string" },
    "art": {
      "type": "string",
      "description": "Relative path to face art asset (assets/...). Omit for template-only cards."
    },
    "image": {
      "type": "string",
      "format": "uri",
      "description": "External card scan URL (shown as the authentic printed card)"
    },
    "back": {
      "type": "string",
      "description": "Relative path to back art if non-default."
    },
    "template_id": {
      "type": "string",
      "description": "Layout template used to render this printing from card data. Omit if art is a finished full-bleed image."
    },
    "variant": {
      "type": "string",
      "description": "Variant label ('promo', 'alt-art', 'full-art')."
    },
    "provenance": {
      "$ref": "provenance.schema.json",
      "description": "Provenance of THIS printing's art. Falls back to game.default_provenance if omitted."
    }
  }
}
