{
  "$comment": "Phase-B typechecking (LANGUAGES.md): npm run typecheck checks every core JS module. Strict mode is the next ratchet after the existing public functions gain JSDoc; do not disable checkJs.",
  "compilerOptions": {
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "strict": false,
    "target": "es2022",
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "types": ["node"],
    "skipLibCheck": true
  },
  "include": ["platform/**/*.mjs", "tools/lib/**/*.mjs", "server.mjs"]
}
